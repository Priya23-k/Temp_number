  <!DOCTYPE html>
  <html lang="en">

  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Temp Number Navbar</title>
    <link rel="icon" href="/image/logo.png" type="image/png">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/service.css">
  </head>

  <body>
    <!-- Title Section -->
    <div id="navbar-placeholder"></div>

    <!-- Temp Number Section -->
    <div class="container my-5 p-5">
      <div class="row align-items-center">
        <div class="col-md-6">
          <h1 class="fw-bold display-4">Temp Number for <span class="text-warning servicename"></span></h1>
          <p class="text-muted lead">
            Create a <span class="servicename"></span> account without providing your real number using temporary real-sim numbers.
            You can keep your private number secure by using a Temp Number to receive verification SMS messages online from <span class="service-name"></span>.
          </p>
        </div>
        <div class="col-md-6 d-flex rounded justify-content-center">
          <div class="form-card mx-auto p-4 mt-4 w-75">
            <form>
              <!-- Service Field -->
              <div class="mb-3">
                <label for="service" class="form-label">Service</label>
                <div class="input-group">
                  <span class="input-group-text"><i class="fa-solid fa-gear"></i></span>
                  <!-- Dropdown container -->
                  <select id="try_service" class="form-select">
                    <option value="" disabled selected>Select a service</option>
                    <option value="telegram">Telegram</option>
                    <option value="whatsapp">WhatsApp</option>
                  </select>
                </div>
              </div>

              <!-- Country Field -->
              <div class="mb-3">
                <label for="country" class="form-label">Country</label>
                <div class="input-group">
                  <span class="input-group-text"><i class="fa-solid fa-gear"></i></span>
                  <!-- Dropdown container -->
                  <select id="try_country" class="form-select">
                    <option value="" disabled selected>Select a country</option>
                  </select>
                </div>
              </div>

              <!-- Submit Button -->
              <button type="submit" class="btn btn-primary-custom text-white btn-lg w-100 fs-6" id="submit-btn">
                Get Number →
              </button>
            </form>
          </div>
        </div>
      </div>
    </div>

    <!-- What is WhatsApp Section -->
    <div class="container my-5">
      <div class="row align-items-center service-bg-light rounded service-shadow p-4">
        <div class="col-auto">
          <img alt="WhatsApp Icon" class="serviceimage img-fluid service-icon" style="width: 50px;">
        </div>
        <div class="col">
          <h4 class="fw-bold">What is <span class="servicename"></span>?</h4>
          <p class="service-text-muted">
            <span class="servicedescription"></span>
          </p>
        </div>
      </div>
    </div>

    <div class="container my-5 py-5">
      <h2 class="text-center fw-bold">Available Services and Countries</h2>
      <p class="text-center text-muted">
        Use temporary disposable numbers for SMS verification in different countries and for various services.
      </p>

      <!-- Tabs -->
      <ul class="nav nav-tabs justify-content-center" id="tabMenu">
        <li class="nav-item">
          <button class="nav-link" id="services-tab" data-bs-toggle="tab" data-bs-target="#services" type="button">Services</button>
        </li>
        <li class="nav-item">
          <button class="nav-link active" id="countries-tab" data-bs-toggle="tab" data-bs-target="#countries" type="button">Countries</button>
        </li>
      </ul>

      <!-- Tab Content -->
      <div class="tab-content">
        <!-- Services Table -->
        <!-- <div class="tab-pane fade" id="services" role="tabpanel">
            <div class="container">
                <div class="row gy-4">
                    <% services.forEach(service => { %>
                        <div class="col-md-4" onclick="window.location.href='/service?serviceid=<%= service.serviceid %>'">
                            <a href="/service?serviceid=<%= service.serviceid %>" class="card text-center text-decoration-none">
                                <img src="<%= service.serviceimage %>" class="card-img-top img-fluid mt-3" alt="<%= service.servicename %>">
                                <div class="card-body">
                                    <h5 class="card-title"><%= service.servicename %></h5>
                                </div>
                            </a>
                        </div>
                    <% }); %>
                </div>
            </div>
        </div> -->

        <!-- Countries Table -->
        <div class="tab-pane fade show active" id="countries" role="tabpanel">
            <!-- <div class="row">
                <% countries.forEach(country => { %>
                    <div class="col-md-4 col-sm-6 mb-3" onclick="window.location.href='/country?countryid=<%= country.countryid %>'">
                        <a href="/country?countryid=<%= country.countryid %>" class="card text-center text-decoration-none">
                            <img src="<%= country.countryimage %>" class="card-img-top img-fluid" alt="<%= country.countryname %>">
                            <div class="card-body">
                                <h5 class="card-title"><%= country.countryname %></h5>
                            </div>
                        </a>
                    </div>
                <% }); %>
            </div> -->
        </div>
      </div>
    </div>
    <div id="footer"></div>
    <script>
      // Function to get query parameter from the URL
      function getQueryParam(param) {
      const urlParams = new URLSearchParams(window.location.search);
      return urlParams.get(param);
  }

  // Redirect function
  function redirectToCombines(countryid) {
      const serviceid = getQueryParam('serviceid'); // Get countryid from URL

      if (!serviceid) {
          console.error("service ID not found in the URL.");
          alert("Please select a service first."); // Optional: Alert the user
          return;
      }

      // Construct the URL for redirection
      const url = `/views/combine.ejs?serviceid=${serviceid}&countryid=${countryid}`;
      window.location.href = url;
  }
    </script>
  <script src="/js/country.js"></script>
    <script src="/js/script.js"></script>
    <script src="/js/service.js"></script>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

  </body>

  </html>
